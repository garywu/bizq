{
  "name": "@bizq/api-gateway",
  "version": "1.0.0",
  "description": "Cloudflare-native API Gateway using Hono framework",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "test": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "@cloudflare/vitest-pool-workers": "^0.1.0",
    "@types/node": "^20.10.5",
    "typescript": "^5.3.3",
    "vitest": "^1.0.0",
    "wrangler": "^3.22.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}